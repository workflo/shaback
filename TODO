Restore:
-----------------------
- Search for different versions of a given file (by name)
- Build Knoppix with shaback

Library:
-----------------------

Backup:
-----------------------
- Pinuts:
  - lock/unlock
  - Split large files
  - ACL
  - Write report file after backup
- Different cipher algorithms:
  - Twofish
  - AES

Encryption:
-----------------------
- Make sure the specified password is correct

Repository:
-----------------------

Garbage collection:
-----------------------

Funny optimizations:
-----------------------
- Sort dir entries by inode

Config:
-----------------------
- Allow different hashing algorithms (SHA-1, SHA-256)
- Lua:
  - Determine hostname
  - system()
  - postBackup hook: write report, rsync repository, send email etc.
  - backupError hook
  - send email

Restore GUI
-----------------------
- Have a Java/Swing UI
- Call CLI shaback via IPC (stdin/stdout)
- Local cache for tree files
